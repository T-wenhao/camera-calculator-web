# 光学方案计算器

用于在光学方案设计阶段，为指定相机筛选镜头并验证成像与安装方案是否可行。

## Language

**光学方案**:
在指定相机前提下，由镜头焦距、光圈、对焦距离、安装姿态和安装位置共同构成的成像配置，并通过视场、目标分辨率和景深进行验证。
_Avoid_: 仅镜头选型

**指定相机**:
在计算开始前已经确定型号及传感器规格的相机；计算器不负责推荐或替换相机。
_Avoid_: 候选相机、推荐相机

**相机目录**:
保存内置相机的受控规格；传感器宽度由横向像素数和像元尺寸唯一推导，最高行频未知时运动采样结果不得默认通过。
_Avoid_: 镜头目录

**自定义相机**:
由用户提供横向像素数和像元尺寸的指定相机；其他目录字段可以缺省，但缺省信息必须在相关结果中标记为无法判定。
_Avoid_: 推荐相机

**近端工作面**:
目标景深中最靠近相机的物面，由其物距和必须覆盖的视场宽度共同定义。
_Avoid_: 最近视场、前景

**对焦面**:
镜头实际合焦的物面；用户可以指定对焦距离，也可以留空交由计算器自动求解，不等同于整个工作距离范围。
_Avoid_: 工作距离

**自动对焦位置**:
对焦距离留空时，使近端和远端工作面的弥散圆要求相等、并令景深所需 F 值最小的有限共轭精确解；不是近远物距的算术中点。
_Avoid_: 工作区间中点、经验对焦位置

**远端工作面**:
目标景深中最远离相机的物面，由其物距和必须覆盖的视场宽度共同定义。
_Avoid_: 最远视场、后景

**目标工作区间**:
用户输入的近端工作面到远端工作面的连续物距范围，是景深覆盖判定的目标，不是计算器自动求得的相机安装位置区间。
_Avoid_: 对焦距离、可行安装距离范围

**计算景深范围**:
按照设计弥散圆计算得到的清晰近界至清晰远界，是需要与目标工作区间进行比较的输出结果。
_Avoid_: 目标工作区间、可行安装距离范围

**目标景深**:
从近端工作面到远端工作面均需满足清晰度标准的连续范围；所需光圈由该范围反算。
_Avoid_: 当前景深

**景深所需最低 F 值**:
由目标景深和设计弥散圆反算得到的 F 值下限；工作 F 值不小于此值时，理论景深才能覆盖目标区间。
_Avoid_: 建议工作光圈

**最大允许 F 值**:
用户可选设置的不希望继续收小光圈的硬约束，默认 F/11；反算所需 F 值超过该上限时必须判为不满足，不能把结果截断成 F/11。
_Avoid_: 最小光圈直径、反算光圈

**建议工作光圈**:
同时满足景深所需最低 F 值与最大允许 F 值的工作设定；当前有 F 值上限且方案可行时，按该设定输出景深与衍射提示。
_Avoid_: 景深所需最低 F 值

**设计弥散圆**:
直径取指定相机像元尺寸的 2 倍，是反算所需光圈以及判断目标景深是否满足的正式清晰度标准。
_Avoid_: 2 倍景深

**参考弥散圆**:
直径取指定相机像元尺寸的 3 倍，仅用于展示较宽松的参考景深，不参与方案通过判定。
_Avoid_: 3 倍景深

**衍射风险提示**:
使用明确标注的 550 nm 可见光参考波长估算理论衍射斑，仅提示所需光圈可能损失分辨率，不参与方案通过判定。
_Avoid_: 衍射失败、最终衍射验收

**焦距推荐**:
根据指定相机、三个工作面及其视场约束，计算可行焦距范围并对标准焦距进行排序；先淘汰视场、分辨率或最大允许 F 值不满足的候选，再优先无衍射风险提示、焦距更长且所需 F/# 更小的候选，并展示全部排序原因。该阶段不承诺具体镜头型号或料号。
_Avoid_: 镜头型号推荐、镜头料号推荐

**视场设计余量**:
应用于近端和远端所需视场的显式工程百分比；默认 10% 且允许设为 0，不属于光学公式。
_Avoid_: 隐藏余量、实际视场余量

**像素当量**:
指定工作面上一个相机像素对应的物方长度，以 mm/pixel 表示；近端工作面、对焦面和远端工作面分别计算。
_Avoid_: 目标分辨率、相机分辨率

**横向像素当量**:
线阵传感器排列方向上一个像素对应的物方长度，由光学放大倍率决定。
_Avoid_: 纵向采样间距

**纵向采样间距**:
目标相对线阵相机运动方向上相邻两行采样之间的物方距离，由运行速度和实际行频决定。
_Avoid_: 横向像素当量、像素当量

**所需行频**:
为在最高运行速度下达到指定纵向采样间距所需的相机行扫描频率，必须与指定相机的最高行频比较。
_Avoid_: 帧率、横向分辨率

**设计最高行频**:
第一版用于运动采样判定的单一相机行频上限，来自相机目录或用户手动填写；不细分像素格式和带宽模式，缺省时结果为无法判定。
_Avoid_: 无条件最高行频、帧率

**理论行周期**:
所需行频的倒数，是单行采集可用时间的理论上限；它不是指定相机的实际曝光时间。
_Avoid_: 最大曝光时间、触发脉宽

**光学方案参数表**:
汇总相机规格、设计输入、光学选型、三个工作面计算结果以及运动采样与提示的最终交付表。
_Avoid_: 传感器参数

**待完善输入**:
缺少必要数据或输入关系无效，尚不能执行焦距推荐。

**存在可行方案**:
至少一个标准焦距满足全部硬约束，且不存在阻止工程判断的数据缺口。

**可行但需复核**:
硬约束已经满足，但仍有衍射风险、相机规格缺失或需要具体镜头数据确认的事项。

**无可行方案**:
没有任何标准焦距同时满足全部硬约束，必须明确指出发生冲突的约束。

**结果已过期**:
用户修改输入但尚未再次执行计算时的界面状态；保留并明确标记上一次有效结果，同时暂停 PNG 和 PDF 导出，重新计算成功后解除。
_Avoid_: 实时结果、自动重算

**分辨率要求**:
用户可选填写的最大允许像素当量，是焦距候选能否满足检测需求的硬约束。
_Avoid_: 像素当量、目标分辨率

**理论选型值**:
使用有限共轭理想光学模型得到、尚未经过具体镜头数据或实物标定修正的计算结果。
_Avoid_: 实测值、最终验收值

**2D 成像方案**:
围绕指定相机完成焦距推荐、视场与像素当量计算、目标景深反算以及光学布置结果输出的独立工作流，是当前重构的验收范围；不包含候选安装点验证或 CAD 姿态辅助。
_Avoid_: 3D 光学方案

**3D 三角测量方案**:
围绕相机与激光器的基线、夹角、开窗及 Z 向精度进行设计的独立工作流；产品保留入口，但不属于当前 2D 重构的验收范围。
_Avoid_: 2D 成像方案
