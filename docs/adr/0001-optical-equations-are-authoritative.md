# 以明确的光学方程作为数值依据

计算器的验收数值必须来自有明确物理含义的光学方程和显式参数，不把旧工作簿或当前网页的既有实现直接当作数值依据。第一阶段使用有限共轭理想薄透镜方程和放大倍率进行理论选型，不使用远距离速算近似；工作距离必须声明测量基准。目标景深是硬判定；衍射使用明确标注的 550 nm 可见光参考波长进行理论估算，只产生风险提示，不参与通过判定。工作簿只作为流程与输出字段参考。具体镜头确定后，最终结果必须由厂商的主平面、工作距离—放大倍率、畸变与成像质量数据或实物标定修正；缺少这些数据时必须标记为理论选型值，不得宣称为实测值或最终验收值。
